[project]
name = "aldergrove-tools"
version = "0.1.0"
description = "Utilidades de texto para envolver, sangrar y limpiar cadenas"
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["texto", "wrap", "acentos", "titulo", "resumen"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
